language: c++
compiler:
  - clang
  - gcc
before_install:
  - git submodule update --init --recursive
install:
  - cmake -E make_directory build64
  - cd build64
  - cmake -DCMAKE_INSTALL_PREFIX=bin ..
  - cd ..
script: cmake --build build64 --config Release . && ./build64/bin/test/Glare_unit_test
